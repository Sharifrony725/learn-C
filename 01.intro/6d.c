//Write a program that prints an integer variable in a sentence. (Initialize and Print)
#include <stdio.h>
int main(){
    int myNum = 134;
    printf("My Integer variable is: %d", myNum);
    return 0;
}
